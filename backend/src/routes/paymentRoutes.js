// backend/src/routes/paymentRoutes.js
// Este arquivo foi removido pois a rota /create-payment (Checkout Pro) foi descontinuada.
// As novas rotas de pagamento estão em pixRoutes.js e cardPaymentRoutes.js.
const express = require('express');
const router = express.Router();

// Rota vazia para evitar erros de importação se ainda for referenciada em algum lugar
// Mas o ideal é remover a referência no index.js
module.exports = router;
